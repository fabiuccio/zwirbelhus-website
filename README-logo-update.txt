Zwirbelhus logo update

Put the logo file here in the repo:
  assets/img/logo-zwirbelhus.png

Changed files:
  index.html
  galerie.html
  theme.css
  icons.js copied unchanged

The navigation and footer now use the PNG logo instead of the old inline SVG + text.
The active data-theme='sonne' palette was tuned to match the generated logo: teal/turquoise as primary, orange as accent, with softer aqua/peach backgrounds.
